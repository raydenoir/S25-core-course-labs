- name: Deploy Python Application
  hosts: all
  become: true
  vars_files:
    - ../vars/app_python.yml
  roles:
    - docker       # Ensures Docker is installed and running.
    - web_app      # Deploys the web application.